spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3307/webide
    username: root
    password: root

  jpa:
    hibernate:

      ddl-auto: update

    show-sql: true


